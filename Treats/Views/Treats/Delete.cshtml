@{
  Layout = "_Layout";
}

@model Treats.Models.Treat

<h2>Are you sure you want to Delete this Treat from the Database?</h2>

@Html.DisplayNameFor(model => model.TreatName): @Html.DisplayFor(model => model.TreatName)

@using (Html.BeginForm())
{
  <input type="submit" value="Delete Treat" />
}

@Html.ActionLink("Back to Treat Details", "Details", new { id = Model.TreatId })